general {
  output_format = "i3bar"
  colors = true
  interval = 1
  markup = "pango"
}

order += "volume master"
order += "cpu_usage"
order += "memory"
order += "battery 0"
order += "time"

cpu_usage {
  format = "🖥️ <span color='#5599ff'>%usage</span>"
}

battery 0 {
  format = "🔋 <span color='#66ff33'>%percentage</span>"
  status_chr = "⚡ "
  status_bat = "🔋 "
  status_full = "✅ "
  status_idle = "🟢 "
  path = "/sys/class/power_supply/BAT%d/uevent"
  low_threshold = 20
}

volume master {
        format = "🔊 <span color='#bb9af7'>%volume</span>"
        format_muted = "🔇 <span color='#f7768e'>muted (%volume)</span>"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}

memory {
  format = "💾 <span color='#ffdd00'>%percentage_used</span>"
}

time {
  format = "🕐 <span color='#00ccff'>%d-%m %H:%M:%S</span>"
}
